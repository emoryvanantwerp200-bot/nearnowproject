{
  "name": "nearnowproject",
  "private": true,
  "type": "module",
  "scripts": {
    "mobile:prepare": "node scripts/prepare-mobile-web.cjs",
    "mobile:init": "npm run mobile:prepare && npx cap add android && npx cap add ios",
    "mobile:sync": "npm run mobile:prepare && npx cap sync",
    "mobile:android": "npm run mobile:sync && npx cap open android",
    "mobile:ios": "npm run mobile:sync && npx cap open ios"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@netlify/database": "^1.0.0",
    "@netlify/identity": "^1.2.0",
    "drizzle-orm": "^1.0.0-beta.22"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1",
    "@netlify/functions": "^5.2.2",
    "drizzle-kit": "^1.0.0-beta.22"
  }
}
